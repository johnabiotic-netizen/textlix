// SMSPVA was evaluated but not used — rental feature uses GrizzlySMS (primary) + OnlineSim (backup).
// Reason: SMSPVA rental is service-specific (one app per number); GrizzlySMS service=full
// allows receiving from any app on a single rented number.
module.exports = {};
